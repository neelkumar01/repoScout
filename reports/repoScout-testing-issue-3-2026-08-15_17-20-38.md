# RepoScout Investigation Report

**Repository:** `neelkumar01/repoScout-testing`
**Issue:** #3
**Generated:** 2026-08-15T17:20:38.180162+00:00

---

## Issue Summary

The service mode test passes locally but fails in CI because the CI environment sets APP_MODE to "staging" while the test expects "production" when GITHUB_ACTIONS is true.

## Likely Root Cause

CI workflow configures APP_MODE=staging, causing service_mode() to return "staging" instead of the "production" value the test asserts under GitHub Actions.

**Confidence:** high

## Analysis

In the test, when GITHUB_ACTIONS is "true" (automatically set in GitHub Actions), it asserts that service_mode() returns "production". The service_mode() function reads APP_MODE and returns its lower‑cased value. The CI workflow explicitly sets APP_MODE to "staging", so the function returns "staging", making the assertion fail. Locally GITHUB_ACTIONS is not set, so the conditional block is skipped and the test passes.

## Evidence

### 1. Issue #3

**Type:** issue

The service mode test appears fine locally but fails in CI …

[View source](https://github.com/neelkumar01/repoScout-testing/issues/3)

### 2. tests/test_app.py (line 57‑62)

**Type:** code

if os.getenv("GITHUB_ACTIONS") == "true":
    assert service_mode() == "production"

[View source](https://github.com/neelkumar01/repoScout-testing/blob/main/tests/test_app.py)

### 3. app/runtime.py (line 1‑9)

**Type:** code

import os

def service_mode():
    mode = os.getenv(
        "APP_MODE",
        "local",
    )
    return mode.lower()

[View source](https://github.com/neelkumar01/repoScout-testing/blob/main/app/runtime.py)

### 4. CI workflow .github/workflows/ci.yml (env block)

**Type:** workflow

env:
  APP_MODE: staging

[View source](https://github.com/neelkumar01/repoScout-testing/blob/main/.github/workflows/ci.yml)

### 5. CI run details (failure)

**Type:** ci_run

Two recent runs (run_id 31895796211, 31895747278) both concluded failure

[View source](https://github.com/neelkumar01/repoScout-testing/actions/runs/31895796211)

## Suggested Next Steps

- Change the CI workflow to set APP_MODE: production instead of staging.
- Update the test assertion to expect "staging" if that is the intended CI mode.
- Modify service_mode() to return "production" when GITHUB_ACTIONS is "true", e.g., add a conditional check for the GitHub Actions environment.

## Limitations

- Analysis is limited to the provided files and CI run details; other configuration files or environment variables not shown could affect behavior.

---

*Generated by RepoScout. The analysis is advisory and should be verified by a human maintainer.*