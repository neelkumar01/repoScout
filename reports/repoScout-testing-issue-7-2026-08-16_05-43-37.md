# RepoScout Investigation Report

**Repository:** `neelkumar01/repoScout-testing`
**Issue:** #7
**Generated:** 2026-08-16T05:43:37.178018+00:00

---

## Issue Summary

Usernames are suddenly stored in uppercase. The application previously normalised usernames to lowercase before persisting them, but recent stored values are uppercase and the change causing this behaviour is unknown.

## Likely Root Cause

The normalize_username function was changed to return an uppercase string instead of a lowercase one, causing all usernames to be stored in uppercase.

**Confidence:** high

## Analysis

The application likely calls normalize_username wherever a username is created or updated. Prior to 2026‑08‑16 05:25:58 UTC the function lower‑cased the input, matching expected behaviour. The refactor commit switched the case conversion to upper(), so every subsequent username is stored in uppercase. No other code changes appear required for this symptom.

## Evidence

### 1. Current implementation of app/normalizer.py

**Type:** file

normalize_username returns value.strip().upper()

[View source](https://github.com/neelkumar01/repoScout-testing/blob/main/app/normalizer.py)

### 2. Earlier version of app/normalizer.py (commit 99c2916e)

**Type:** file

normalize_username returned value.strip().lower()

[View source](https://github.com/neelkumar01/repoScout-testing/blob/99c2916eccb535eb74cd1e566eecec0e935e4a0b/app/normalizer.py)

## Suggested Next Steps

- Revert or amend the change: modify normalize_username to use .lower() again or document the intended policy.
- Add a unit test asserting normalize_username("  FooBar ") == "foobar" to prevent future regressions.
- Review and clarify the commit message to reflect that usernames should be normalised to lowercase.

## Limitations

- The investigation assumes all username persistence goes through normalize_username; other code paths could affect stored values but were not found in the limited search.
- No CI or runtime logs were examined, as the issue appears to be a code‑logic regression without failing tests.

---

*Generated by RepoScout. The analysis is advisory and should be verified by a human maintainer.*